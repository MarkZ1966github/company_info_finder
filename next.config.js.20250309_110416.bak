/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Custom server port
  webpack5: true,
  webpack: (config) => {
    return config;
  },
  serverRuntimeConfig: {
    PORT: 5000,
  },
  env: {
    PORT: 5000,
  },
};

module.exports = nextConfig;